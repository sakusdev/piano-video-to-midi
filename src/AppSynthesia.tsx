import { useEffect, useMemo, useRef, useState } from "react";

type Rect={x:number;y:number;w:number;h:number};
type Key=Rect&{midi:number;isBlack:boolean};
type Ev={midi:number;startMs:number;endMs:number;velocity:number};
type Active={startMs:number;strength:number};
type Mode="hybrid"|"falling"|"glow";
const BLACK=new Set([1,3,6,8,10]);
const N=["C","C#","D","D#","E","F","F#","G","G#","A","A#","B"];
const clamp=(n:number,min:number,max:number)=>Math.max(min,Math.min(max,n));
const isBlack=(m:number)=>BLACK.has(m%12);
function whiteIndexToMidi(idx:number){let c=-1;for(let m=21;m<=108;m++){if(!isBlack(m)){c++;if(c===idx)return m;}}return 108;}
function buildKeys(r:Rect){const out:Key[]=[];const ww=r.w/52;let wi=0;for(let m=21;m<=108;m++){if(!isBlack(m)){out.push({midi:m,isBlack:false,x:r.x+wi*ww,y:r.y+r.h*.58,w:ww,h:r.h*.38});wi++;}}const after=new Set(["A","C","D","F","G"]);for(let i=0;i<51;i++){const lm=whiteIndexToMidi(i);if(!after.has(N[lm%12]))continue;const m=lm+1;if(m<21||m>108)continue;out.push({midi:m,isBlack:true,x:r.x+(i+1)*ww-ww*.3,y:r.y+r.h*.04,w:ww*.6,h:r.h*.48});}return out.sort((a,b)=>Number(a.isBlack)-Number(b.isBlack));}
function hsv(r:number,g:number,b:number){r/=255;g/=255;b/=255;const max=Math.max(r,g,b),min=Math.min(r,g,b),d=max-min;let h=0;if(d){if(max===r)h=((g-b)/d)%6;else if(max===g)h=(b-r)/d+2;else h=(r-g)/d+4;h*=60;if(h<0)h+=360;}return{h,s:max===0?0:d/max,v:max};}
function hue(hex:string){const s=hex.replace("#","");const n=parseInt(s.length===3?s.split("").map(c=>c+c).join(""):s,16);return hsv((n>>16)&255,(n>>8)&255,n&255).h;}
function hueDist(a:number,b:number){const d=Math.abs(a-b)%360;return d>180?360-d:d;}
function img(ctx:CanvasRenderingContext2D,r:Rect){const x0=clamp(Math.floor(r.x),0,ctx.canvas.width-1),y0=clamp(Math.floor(r.y),0,ctx.canvas.height-1);const x1=clamp(Math.ceil(r.x+r.w),0,ctx.canvas.width),y1=clamp(Math.ceil(r.y+r.h),0,ctx.canvas.height);if(x1<=x0||y1<=y0)return null;return ctx.getImageData(x0,y0,x1-x0,y1-y0);}
function glow(ctx:CanvasRenderingContext2D,r:Rect){const im=img(ctx,r);if(!im)return 0;const d=im.data;let sum=0,hot=0,c=0;for(let y=0;y<im.height;y+=2)for(let x=0;x<im.width;x+=2){const i=(y*im.width+x)*4;const l=.2126*d[i]+.7152*d[i+1]+.0722*d[i+2];sum+=l;if(l>145)hot++;c++;}return sum/Math.max(1,c)+(hot/Math.max(1,c))*80;}
function line(ctx:CanvasRenderingContext2D,k:Key,y:number,h:number,hues:number[],strict:number){const im=img(ctx,{x:k.x+k.w*.12,y,w:k.w*.76,h});if(!im)return{ratio:0,strength:0};const d=im.data;let hit=0,str=0,c=0;const minS=.18+strict*.006,win=Math.max(12,34-strict*.25);for(let i=0;i<d.length;i+=4){const p=hsv(d[i],d[i+1],d[i+2]);const ok=p.s>=minS&&p.v>=.28&&hues.some(hh=>hueDist(p.h,hh)<=win);if(ok){hit++;str+=p.s*105+p.v*35;}c++;}return{ratio:hit/Math.max(1,c),strength:str/Math.max(1,c)};}
function varLen(v:number){let b=v&0x7f;const out:number[]=[];while((v>>=7)){b<<=8;b|=(v&0x7f)|0x80;}while(true){out.push(b&0xff);if(b&0x80)b>>=8;else break;}return out;}
const ascii=(s:string)=>[...s].map(c=>c.charCodeAt(0));const u16=(n:number)=>[(n>>>8)&255,n&255];const u32=(n:number)=>[(n>>>24)&255,(n>>>16)&255,(n>>>8)&255,n&255];
function midi(events:Ev[],bpm=120){const ppq=480,tpm=ppq*bpm/60000;type R={tick:number;order:number;bytes:number[]};const raw:R[]=[];const tempo=Math.round(60000000/bpm);raw.push({tick:0,order:0,bytes:[255,81,3,(tempo>>>16)&255,(tempo>>>8)&255,tempo&255]});for(const e of events){const st=Math.max(0,Math.round(e.startMs*tpm)),et=Math.max(st+1,Math.round(e.endMs*tpm));raw.push({tick:st,order:2,bytes:[144,clamp(e.midi,0,127),clamp(e.velocity,1,127)]});raw.push({tick:et,order:1,bytes:[128,clamp(e.midi,0,127),0]});}raw.sort((a,b)=>a.tick-b.tick||a.order-b.order||a.bytes[1]-b.bytes[1]);const tr:number[]=[];let cur=0;for(const e of raw){tr.push(...varLen(Math.max(0,e.tick-cur)),...e.bytes);cur=e.tick;}tr.push(0,255,47,0);return new Uint8Array([...ascii("MThd"),...u32(6),...u16(0),...u16(1),...u16(ppq),...ascii("MTrk"),...u32(tr.length),...tr]);}
function merge(es:Ev[],min:number,gap=22){const s=[...es].filter(e=>e.endMs-e.startMs>=min).sort((a,b)=>a.midi-b.midi||a.startMs-b.startMs);const out:Ev[]=[];for(const e of s){const l=out[out.length-1];if(l&&l.midi===e.midi&&e.startMs-l.endMs<=gap){l.endMs=Math.max(l.endMs,e.endMs);l.velocity=Math.max(l.velocity,e.velocity);}else out.push({...e});}return out.sort((a,b)=>a.startMs-b.startMs||a.midi-b.midi);}
export default function AppSynthesia(){const videoRef=useRef<HTMLVideoElement|null>(null),canvasRef=useRef<HTMLCanvasElement|null>(null),rafRef=useRef<number|null>(null),timerRef=useRef<number|null>(null),analyzing=useRef(false),urlRef=useRef(""),lastT=useRef(-1);const base=useRef(new Map<number,number>()),active=useRef(new Map<number,Active>()),pon=useRef(new Map<number,number>()),poff=useRef(new Map<number,number>()),events=useRef<Ev[]>([]);
const [url,setUrl]=useState(""),[rect,setRect]=useState<Rect|null>(null),[drag,setDrag]=useState<{x:number;y:number}|null>(null),[dragRect,setDragRect]=useState<Rect|null>(null),[running,setRunning]=useState(false),[status,setStatus]=useState("動画を読み込んで鍵盤範囲を調整"),[count,setCount]=useState(0);const[mode,setMode]=useState<Mode>("hybrid"),[threshold,setThreshold]=useState(18),[lineOffset,setLineOffset]=useState(14),[lineHeight,setLineHeight]=useState(4),[strict,setStrict]=useState(10),[frames,setFrames]=useState(2),[minMs,setMinMs]=useState(35),[left,setLeft]=useState("#6fb8ff"),[right,setRight]=useState("#9bdc4b"),[split,setSplit]=useState(50);
const keys=useMemo(()=>rect?buildKeys(rect):[],[rect]);const lh=useMemo(()=>hue(left),[left]),rh=useMemo(()=>hue(right),[right]);const cw=canvasRef.current?.width??960,ch=canvasRef.current?.height??540,xMin=-Math.round(cw*.5),xMax=Math.round(cw*.5),wMax=Math.round(cw*1.8);
function estimate(){const c=canvasRef.current;if(!c)return{x:0,y:0,w:960,h:200};return{x:-Math.round(c.width*.03),y:Math.round(c.height*.66),w:Math.round(c.width*1.06),h:clamp(Math.round(c.height*.3),38,Math.round(c.height*.45))};}
function update(p:Partial<Rect>){const c=canvasRef.current;if(!c)return;const n={...(rect??estimate()),...p};n.x=clamp(Math.round(n.x),-Math.round(c.width*.5),Math.round(c.width*.5));n.y=clamp(Math.round(n.y),0,Math.max(0,c.height-10));n.w=clamp(Math.round(n.w),10,Math.round(c.width*1.8));n.h=clamp(Math.round(n.h),10,c.height-n.y);setRect(n);}
function reset(){base.current.clear();active.current.clear();pon.current.clear();poff.current.clear();events.current=[];lastT.current=-1;setCount(0);}function close(m:number,a:Active,end:number){if(end-a.startMs<minMs)return;events.current.push({midi:m,startMs:a.startMs,endMs:end,velocity:clamp(Math.round(44+a.strength*1.15),35,120)});setCount(events.current.length);}function draw(){const v=videoRef.current,c=canvasRef.current,ctx=c?.getContext("2d");if(!v||!c||!ctx||!c.width||!c.height)return;ctx.clearRect(0,0,c.width,c.height);ctx.drawImage(v,0,0,c.width,c.height);if(rect){ctx.save();ctx.strokeStyle="#22d3ee";ctx.lineWidth=2;ctx.strokeRect(rect.x,rect.y,rect.w,rect.h);const hy=rect.y-rect.h*(lineOffset/100);ctx.strokeStyle="#facc15";ctx.beginPath();ctx.moveTo(rect.x,hy);ctx.lineTo(rect.x+rect.w,hy);ctx.stroke();for(const k of keys){ctx.globalAlpha=active.current.has(k.midi)?1:k.isBlack?.9:.55;ctx.strokeStyle=active.current.has(k.midi)?"#a3e635":k.isBlack?"#ef4444":"#06b6d4";ctx.lineWidth=active.current.has(k.midi)?3:1;ctx.strokeRect(k.x,k.y,k.w,k.h);}ctx.restore();}if(dragRect){ctx.save();ctx.strokeStyle="#facc15";ctx.lineWidth=2;ctx.strokeRect(dragRect.x,dragRect.y,dragRect.w,dragRect.h);ctx.restore();}}
function resume(){const v=videoRef.current;if(!v||!analyzing.current||v.ended||timerRef.current!==null)return;timerRef.current=window.setTimeout(()=>{timerRef.current=null;if(!analyzing.current||!videoRef.current||videoRef.current.ended)return;videoRef.current.play().catch(()=>setStatus("スマホ側で停止。再生ボタンで継続"));if(rafRef.current===null)rafRef.current=requestAnimationFrame(loop);},180);}function loop(){rafRef.current=null;const v=videoRef.current,c=canvasRef.current,ctx=c?.getContext("2d");if(!v||!c||!ctx||!rect||!analyzing.current)return;if(v.paused&&!v.ended){resume();rafRef.current=requestAnimationFrame(loop);return;}if(v.currentTime===lastT.current){rafRef.current=requestAnimationFrame(loop);return;}lastT.current=v.currentTime;ctx.drawImage(v,0,0,c.width,c.height);const now=v.currentTime*1000,hy=rect.y-rect.h*(lineOffset/100),sx=rect.x+rect.w*(split/100);for(const k of keys){const gs=glow(ctx,k),b=base.current.get(k.midi)??gs,gd=gs-b,ls=line(ctx,k,hy,lineHeight,[k.x+k.w*.5<sx?lh:rh],strict);const fo=ls.ratio>threshold/100,ff=ls.ratio<threshold/260,go=gd>threshold,gf=gd<threshold*.38,on=mode==="hybrid"?fo||go:mode==="falling"?fo:go,off=mode==="hybrid"?ff&&gf:mode==="falling"?ff:gf,str=Math.max(Math.max(0,gd),ls.strength),a=active.current.get(k.midi);if(!a&&gd<threshold*.65)base.current.set(k.midi,b*.97+gs*.03);if(!a){if(on){const n=(pon.current.get(k.midi)??0)+1;pon.current.set(k.midi,n);poff.current.set(k.midi,0);if(n>=frames){active.current.set(k.midi,{startMs:Math.max(0,now-(frames-1)*16.7),strength:str});pon.current.set(k.midi,0);}}else pon.current.set(k.midi,0);}else{a.strength=Math.max(a.strength,str);if(off){const n=(poff.current.get(k.midi)??0)+1;poff.current.set(k.midi,n);if(n>=frames){close(k.midi,a,Math.max(a.startMs+1,now-(frames-1)*16.7));active.current.delete(k.midi);poff.current.set(k.midi,0);}}else poff.current.set(k.midi,0);}}draw();if(!v.ended)rafRef.current=requestAnimationFrame(loop);else{analyzing.current=false;setRunning(false);setStatus("解析完了");}}
async function start(){const v=videoRef.current;if(!v||!rect){setStatus("先に動画と鍵盤範囲を設定");return;}reset();analyzing.current=true;setRunning(true);setStatus("解析中");try{v.setAttribute("playsinline","true");await v.play();}catch{setStatus("再生ボタンを押してからstart");}if(rafRef.current===null)rafRef.current=requestAnimationFrame(loop);}function stop(){analyzing.current=false;if(timerRef.current!==null)window.clearTimeout(timerRef.current);timerRef.current=null;if(rafRef.current!==null)cancelAnimationFrame(rafRef.current);rafRef.current=null;videoRef.current?.pause();setRunning(false);setStatus("停止");}function exportMidi(){const now=(videoRef.current?.currentTime??0)*1000;for(const[m,a]of active.current)close(m,a,Math.max(now,a.startMs+minMs));active.current.clear();const es=merge(events.current,minMs);if(!es.length){setStatus("ノートなし");return;}const u=URL.createObjectURL(new Blob([midi(es)],{type:"audio/midi"}));const a=document.createElement("a");a.href=u;a.download="piano-video.mid";a.click();URL.revokeObjectURL(u);setCount(es.length);setStatus(`MIDI出力完了: ${es.length} notes`);}function sync(){const v=videoRef.current,c=canvasRef.current;if(!v||!c)return;const sc=Math.min(1,960/(v.videoWidth||1280));c.width=Math.floor((v.videoWidth||1280)*sc);c.height=Math.floor((v.videoHeight||720)*sc);const ctx=c.getContext("2d");if(ctx){ctx.drawImage(v,0,0,c.width,c.height);setRect(estimate());}draw();}function point(e:React.PointerEvent<HTMLCanvasElement>){const c=canvasRef.current!,r=c.getBoundingClientRect();return{x:((e.clientX-r.left)/r.width)*c.width,y:((e.clientY-r.top)/r.height)*c.height};}useEffect(()=>{draw();},[rect,lineOffset,lineHeight,split,keys.length]);useEffect(()=>()=>{analyzing.current=false;if(timerRef.current!==null)window.clearTimeout(timerRef.current);if(rafRef.current!==null)cancelAnimationFrame(rafRef.current);if(urlRef.current)URL.revokeObjectURL(urlRef.current);},[]);
return <div className="app-shell"><div className="app-container"><div><h1 className="app-title">Perfect Piano Video → MIDI</h1><p className="app-subtitle">Synthesia端見切れ対応 / W 180%まで可</p></div><div className="layout-grid"><div className="video-panel"><input className="file-input" type="file" accept="video/*" onChange={e=>{const f=e.target.files?.[0];if(!f)return;if(urlRef.current)URL.revokeObjectURL(urlRef.current);const u=URL.createObjectURL(f);urlRef.current=u;setUrl(u);setRect(null);setDragRect(null);reset();setStatus("動画読み込み完了。仮配置後にX/Wを調整");}}/><video ref={videoRef} src={url} controls playsInline preload="auto" className="video-preview" onLoadedMetadata={sync} onSeeked={draw} onPause={()=>analyzing.current?resume():draw()} onPlay={()=>{if(analyzing.current&&rafRef.current===null)rafRef.current=requestAnimationFrame(loop);}}/><canvas ref={canvasRef} className="canvas-preview" onPointerDown={e=>{const p=point(e);setDrag(p);setDragRect({x:p.x,y:p.y,w:1,h:1});}} onPointerMove={e=>{if(!drag)return;const p=point(e);setDragRect({x:Math.min(p.x,drag.x),y:Math.min(p.y,drag.y),w:Math.abs(p.x-drag.x),h:Math.abs(p.y-drag.y)});}} onPointerUp={()=>{if(dragRect&&dragRect.w>50&&dragRect.h>10){setRect(dragRect);setStatus("鍵盤範囲設定完了");}setDrag(null);}}/><div className="button-grid"><button className="btn" onClick={()=>{setRect(estimate());setStatus("仮配置。端が見切れる場合はXをマイナス、Wを大きく");}}>鍵盤範囲を仮配置</button><button className="btn" onClick={draw}>プレビュー更新</button></div></div><div className="control-panel"><div className="status-box">{status}</div><label className="control-group">検出モード<select className="control-input" value={mode} onChange={e=>setMode(e.target.value as Mode)}><option value="hybrid">Hybrid</option><option value="falling">Falling</option><option value="glow">Glow</option></select></label><div className="status-box">鍵盤範囲 微調整</div><label className="control-group">X {rect?.x??0}<input className="control-input" type="range" min={xMin} max={xMax} value={rect?.x??0} onChange={e=>update({x:Number(e.target.value)})}/></label><label className="control-group">Y {rect?.y??0}<input className="control-input" type="range" min={0} max={ch} value={rect?.y??0} onChange={e=>update({y:Number(e.target.value)})}/></label><label className="control-group">W {rect?.w??0}<input className="control-input" type="range" min={10} max={wMax} value={rect?.w??10} onChange={e=>update({w:Number(e.target.value)})}/></label><label className="control-group">H {rect?.h??0}<input className="control-input" type="range" min={10} max={ch} value={rect?.h??10} onChange={e=>update({h:Number(e.target.value)})}/></label><label className="control-group">左手色<input className="control-input" type="color" value={left} onChange={e=>setLeft(e.target.value)}/></label><label className="control-group">右手色<input className="control-input" type="color" value={right} onChange={e=>setRight(e.target.value)}/></label><label className="control-group">threshold {threshold}<input className="control-input" type="range" min={5} max={60} value={threshold} onChange={e=>setThreshold(Number(e.target.value))}/></label><label className="control-group">line offset {lineOffset}%<input className="control-input" type="range" min={2} max={60} value={lineOffset} onChange={e=>setLineOffset(Number(e.target.value))}/></label><label className="control-group">line height {lineHeight}px<input className="control-input" type="range" min={1} max={16} value={lineHeight} onChange={e=>setLineHeight(Number(e.target.value))}/></label><label className="control-group">color strict {strict}<input className="control-input" type="range" min={0} max={40} value={strict} onChange={e=>setStrict(Number(e.target.value))}/></label><label className="control-group">confirm frames {frames}<input className="control-input" type="range" min={1} max={5} value={frames} onChange={e=>setFrames(Number(e.target.value))}/></label><label className="control-group">min note {minMs}ms<input className="control-input" type="range" min={10} max={160} value={minMs} onChange={e=>setMinMs(Number(e.target.value))}/></label><label className="control-group">左右分割 {split}%<input className="control-input" type="range" min={20} max={80} value={split} onChange={e=>setSplit(Number(e.target.value))}/></label><div className="button-grid"><button className="btn btn-primary" onClick={start} disabled={running}>start</button><button className="btn" onClick={stop} disabled={!running}>stop</button><button className="btn btn-success button-wide" onClick={exportMidi}>export MIDI</button></div><div className="notes-count">notes: {count}</div><p className="help-text">Synthesiaで端がズレる場合: Xをマイナス、Wを100%より大きくして白鍵52個の基準幅を合わせる。</p></div></div></div></div>;
}
